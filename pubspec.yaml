name: pessoa_bonito
description: Beautify ArquivoPessoa.net

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^2.1.1
  html: ^0.15.0
  http: ^0.13.3
  equatable: ^2.0.3
  logger: ^1.1.0
  flutter_spinkit: ^5.1.0
  shared_preferences: ^2.0.7
  json_serializable: ^6.1.4
  json_annotation: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:

  flutter_launcher_icons: "^0.9.2"
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

flutter_icons:
  image_path: "assets/icon/icon.png"
  android: true
  ios: false
